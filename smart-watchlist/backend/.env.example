# Postgres connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=gog_watchlist
# Optional: overrides the PG* vars above if set
DATABASE_URL=
# Auth
JWT_SECRET=change-this-to-something-random

GROQ_API_KEY=
