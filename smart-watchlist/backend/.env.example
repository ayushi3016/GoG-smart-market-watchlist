# Postgres connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=gog_watchlist

# Auth
JWT_SECRET=change-this-to-something-random

# Optional: enables real AI explain/chat (falls back to templates without it)
ANTHROPIC_API_KEY=
